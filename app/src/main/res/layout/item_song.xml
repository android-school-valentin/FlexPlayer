<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="@drawable/song_item_selector">

    <ImageView
        android:id="@+id/song_album_image"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/self_destruct" />

    <TextView
        android:id="@+id/song_name_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_marginStart="4dp"
        android:layout_toEndOf="@id/song_album_image"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Name of the song"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Body1"
        android:textColor="@color/colorTextMain" />

    <TextView
        android:id="@+id/song_artist_name_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/song_name_text"
        android:layout_marginStart="4dp"
        android:layout_toEndOf="@id/song_album_image"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Song artist name"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
        android:textColor="@color/colorSecondaryText" />

    <TextView
        android:id="@+id/song_duration_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/song_menu_more"
        android:text="0:00"/>

    <ImageButton
        android:id="@+id/song_menu_more"
        android:src="@drawable/ic_menu_more_small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:tint="@color/colorIconsSecondary"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"/>

</RelativeLayout>